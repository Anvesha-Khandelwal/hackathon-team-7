services:
  - type: web
    name: turnengine
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn TurnEngine.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0